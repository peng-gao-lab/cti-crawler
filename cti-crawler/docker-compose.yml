version: '3'

services:
  # proxy_pool:
  #   image: jhao104/proxy_pool
  #   container_name: proxy_pool
  #   restart: always
  #   expose:
  #     - '5010'
  #   links:
  #     - proxy_redis:proxy_redis
  #   environment:
  #     DB_CONN: 'redis://proxy_redis:6379'
  # proxy_redis:
  #   image: "redis"
  crawler:
    user: "1000:1000" # creates files using non-root user
    build: .
    image: crawler
    # links:
    #   - proxy_pool:proxy_pool
    environment:
#      - PROXY_HOST=proxy_pool
#      - PROXY_PORT=5010
      - IN_DOCKER_CONTAINER=true
      # - WITH_PROXY=false
    command: python3 main.py
    volumes:
      - ".:/app/crawler:rw" # map current local directory to /app/crawler inside Docker container